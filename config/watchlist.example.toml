[portfolio]
initial_cash = 100000
max_single_position_weight = 0.20
max_equity_drawdown = 0.10
min_cash_weight = 0.20

[data]
primary_provider = "akshare"
fallback_provider = "csv"
cache_db = "runtime/wealth_lab.sqlite3"
poll_seconds = 30

symbols = [
  "000001",
  "600519",
  "300750",
  "688981",
]

[costs]
# Keep these configurable. Verify current broker fee, minimum commission,
# transfer fee, and stamp duty before using in real analysis.
commission_rate = 0.0
min_commission = 0.0
stamp_tax_sell_rate = 0.0

